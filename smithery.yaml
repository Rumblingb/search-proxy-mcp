runtime: "container"
startCommand:
  type: "stdio"
  command: "node"
  args: ["index.js"]
description: "Web search your agent can't get rate-limited off of. DuckDuckGo under the hood, throttling handled, zero API-key signup to start."
